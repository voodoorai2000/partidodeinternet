<% form_for @user do |f| -%>
  <%= f.label :name, "Nombre" %>
	<%= f.text_field :name, :id => :name %>
	
	<%= f.label :url, "Url" %>
	<%= f.text_field :url, :id => :url %>
	
	<%= f.label :region, "Comunidad Autónoma" %>
	<%= f.select :region_id, Region.all.collect {|region| [region.name, region.id]} %>
	
	<%= f.submit "Editar" %>
<% end -%>